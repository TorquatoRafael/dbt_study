sources:
  - name: jaffle_shop 
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
      - name: orders
        config:
          freshness:
            warn_after:
              count: 24
              period: days
            error_after:
              count: 48
              period: days
          loaded_at_field: updated_at
